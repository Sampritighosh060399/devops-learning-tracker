apiVersion: v1
kind: Service

metadata:
  name: devops-learning-tracker-service

spec:
  type: NodePort

  selector:
    app: devops-learning-tracker

  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
